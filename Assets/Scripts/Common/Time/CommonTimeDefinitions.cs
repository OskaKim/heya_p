namespace timeinfo
{
    // timeinfo 관련 정의
    public static class Definitions
    {
        // 기본 시간 재생 상태.
        // true면 재생, false면 일시정지
        public static bool DefaultInteravalTimeConfig = true;
    }
}